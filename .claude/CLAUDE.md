# Claude Code Settings for Bigcapital

## Node.js Version

Always use Node.js 24 (LTS) for this project. Before running any npm/pnpm/node commands:

```bash
nvm use 24
```

## Package Manager

Use `pnpm` for this project.
